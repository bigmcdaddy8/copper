# K9-0120 — YAML-Only Cutover (Remove JSON Runtime Path)

**Phase:** 7 — Reliability & YAML Cutover
**Status:** Completed
**Depends on:** K9-0110 (Error Taxonomy + Actionable Failures)

---

## Goal

Complete migration to YAML v2 by removing JSON runtime loading/migration paths from normal execution and tightening policy/tests/docs accordingly.

---

## New / Modified Files

| File | Change |
|---|---|
| `apps/K9/src/K9/config.py` | Remove `from_json` runtime path (or gate behind explicit legacy-only helper) |
| `apps/K9/src/K9/cli.py` | Remove JSON deprecation branch and `migrate-spec` command (if migration period is over) |
| `apps/K9/tests/test_config.py` | Remove/replace JSON deprecation tests |
| `apps/K9/tests/test_integration.py` | Remove JSON warning tests; keep YAML behavior tests |
| `apps/K9/tests/test_spec_files_policy.py` | Disallow all JSON trade specs |
| `apps/K9/README.md` | YAML-only usage and schema text |
| `docs/K9_TRADE_SPEC_CRITERIA_REFERENCE_v2.md` | Confirm YAML-only runtime support language |

---

## Tasks

### 1. Remove JSON execution path

Ensure `K9 enter` resolves only `.yaml`/`.yml` trade specs.

### 2. Clean migration affordances

If migration is complete, remove `migrate-spec` CLI command and JSON warning logic.

### 3. Tighten spec policy

Update policy tests to fail on any `.json` trade spec in `apps/K9/trade_specs`.

### 4. Refresh docs and tests

Update README and criteria docs to YAML-only language; remove JSON-specific tests and replace with YAML-first assertions.

---

## Acceptance Criteria

- [x] `K9 enter` accepts YAML specs and rejects JSON specs with clear guidance
- [x] No JSON spec files remain under `apps/K9/trade_specs`
- [x] Policy test enforces zero JSON specs
- [x] README and criteria docs are YAML-only and internally consistent
- [x] `uv run pytest apps/K9 -q` — all green
- [x] `uv run ruff check apps/K9` — zero errors

## Validation Snapshot (2026-04-29)

- `uv run pytest apps/K9 -q` -> `74 passed`
- `uv run ruff check apps/K9` -> `All checks passed!`

---

## Notes

- This story is intentionally operationally sensitive; schedule it only after confirming all active automation uses YAML files.
