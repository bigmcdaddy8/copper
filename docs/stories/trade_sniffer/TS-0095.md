# TS-0095 — Edge Case Sandbox Tests

**Phase:** 5 — Demo Scenarios (depends on TS-0090)  
**Status:** Pending

---

## Goal

Test four edge cases from `TRADIER_FAQ.md` that require specific market conditions: nickel pricing behaviour, option expiration timing, after-hours GTC placement, and after-hours stock quotes. Because these cannot all be exercised in a single interactive session, this story delivers:

1. A `tradier_sniffer demo edge_cases [--run <test>]` CLI command for mechanical tests and checklist printing.
2. Shell scripts in `apps/tradier_sniffer/scripts/` for cron-based daily automation.

---

## Architecture: Cron-based Daily Observation Loop

### Shell scripts (`apps/tradier_sniffer/scripts/`)

| Script | Cron schedule (CT) | What it does |
|---|---|---|
| `daily_entry.sh` | `45 8 * * 1-5` | `tradier_sniffer demo scenario1` — places daily SIC entry |
| `expiry_check.sh` | `10 15 * * 1-5` | `tradier_sniffer demo edge_cases --run expiry_timing` |
| `after_hours_check.sh` | `15 15 * * 1-5` | `tradier_sniffer demo edge_cases --run after_hours_quotes` |
| `nickel_test.sh` | Run once manually | `tradier_sniffer demo edge_cases --run nickel_pricing` |
| `cron_setup.sh` | — | Prints crontab entries with CT schedule |

Each script sources `.env`, runs the command, and appends output to `logs/edge_cases_YYYY-MM-DD.log`.

**Timezone:** System clock is `America/Chicago` (CT). CT is always 1 hour behind ET (both CST/CDT and EST/EDT shift together), so the offset is stable year-round. No DST adjustment needed between CT and ET.

---

## Edge Case Sub-tests

### EC-1: Nickel Pricing (`--run nickel_pricing`)

Place a Day STO Limit with a penny-priced limit on a nickel-only option. Observe acceptance, rejection, or silent rounding.

**Observation checklist (observed 2026-04-13):**
- [ ] Order rejected — `reason_description` = `invalid_price`
- [ ] Order accepted — price was silently rounded to nearest nickel
- [x] Order accepted — price as submitted (Tradier does not enforce nickel increment)
  - Symbol: `SPXW260413P05000000`, submitted price: `$0.06`, order status: `ok`, order ID: `27894349`
  - **Note:** sandbox only — production behavior may differ (exchange-level enforcement possible)

### EC-2: Option Expiration Timing (`--run expiry_timing`)

After market close (4:10 PM ET), poll orders/positions for today's 0DTE SPX options.

**Observation checklist:**
- [ ] `get_positions` — option still visible at 4:10 PM; time of disappearance: ______
- [ ] `get_orders` — status changed to `expired` at: ______
- [ ] Approximate delay from 4:00 PM close to `expired` status: ______ minutes

> **Still blocked as of 2026-04-22.** Two sequential failures:
> 1. 2026-04-13–15: `uv: command not found` (PATH fix applied 2026-04-15).
> 2. 2026-04-16–22: `HTTP 400: Invalid parameter, class: the number of legs must be greater than 1` — `httpx`'s `data=` kwarg percent-encodes `[` and `]` in keys like `leg[0][option_symbol]`, causing Tradier's backend to see 0 legs. Fixed 2026-04-22 in `tradier_client.py:_post` — now builds the form body manually, keeping bracket keys literal. EC-2 requires a filled/open 0DTE position from `daily_entry.sh` running successfully first.

### EC-3: After-hours GTC Placement (`--run after_hours_gtc`)

After 4:05 PM ET, place a GTC BTC limit order for an open position.

**Observation checklist (observed 2026-04-15, Wednesday after close):**
- [x] Order accepted — status `ok`, queued for next session
  - Symbol: `SPXW260416P06370000` (1DTE put), order ID: `28150911`
  - GTC orders are accepted after hours — no `market_closed` rejection
- [ ] Order rejected — `reason_description` = `market_closed`
- [ ] Order accepted — status `pending`

> **Complete.** `run_after_hours_gtc` updated 2026-04-15 to use `get_next_expiration` (1DTE+) instead of `get_0dte_expiration`, so the test works after close when today's options have stale/zero quotes. Run manually Thursday evening (after 4:05 PM ET): `uv run tradier_sniffer demo edge_cases --run after_hours_gtc`

### EC-4: After-hours Stock Quotes (`--run after_hours_quotes`)

After 4:05 PM ET, call `GET /markets/quotes?symbols=SPY,QQQ`.

**Observation checklist (observed 2026-04-16 through 2026-04-22, 5 trading days):**
- [x] `last` is the regular-session closing price (no after-hours data surfaced)
  - `has_extended_trade: False`, `has_post_market: False` — consistent across all 5 days for SPY, QQQ, and SPX
  - `bid`/`ask` do update after close but `last` is frozen at session close
  - Sandbox limitation — production behavior may differ
- [ ] `last` price matches after-hours price in Tradier desktop app
- [ ] `extended_trade` or `post_market` sub-object present in response: yes / no

> **Complete (sandbox).** FAQ answer written. Production re-test needed once live account is available.

---

## New / Modified Files

| File | Change |
|---|---|
| `src/tradier_sniffer/tradier_client.py` | Add `get_quotes(symbols: list[str]) -> list[dict]` |
| `src/tradier_sniffer/demo/edge_cases.py` | New — per-test runner functions + checklist printer |
| `src/tradier_sniffer/cli.py` | Add `demo edge_cases [--run TEST]` command |
| `scripts/daily_entry.sh` | New |
| `scripts/expiry_check.sh` | New |
| `scripts/after_hours_check.sh` | New |
| `scripts/nickel_test.sh` | New |
| `scripts/cron_setup.sh` | New |

---

## Tasks

### 1. Add `get_quotes` to `tradier_client.py`

```python
def get_quotes(self, symbols: list[str]) -> list[dict]:
```
`GET /markets/quotes?symbols={comma-joined-symbols}` — always returns a list.

### 2. Create `demo/edge_cases.py`

Module-level function `print_checklists()` — prints all four checklists using Rich without sandbox calls.

Per-test functions (`run_nickel_pricing`, `run_expiry_timing`, `run_after_hours_gtc`, `run_after_hours_quotes`) — each takes `(client, account_id, conn)`, executes the observation, prints findings with Rich, appends an `EventLog` entry, and returns a findings dict.

### 3. Add CLI command

```
tradier_sniffer demo edge_cases [--run nickel_pricing|expiry_timing|after_hours_gtc|after_hours_quotes]
```

No `--run` → print checklists only (no API calls, exits 0).

### 4. Create shell scripts

All under `apps/tradier_sniffer/scripts/`. Each is `chmod +x`.

### 5. Document findings in `TRADIER_FAQ.md`

After running each sub-test, add `**Answer:**` lines to the relevant FAQ questions.

---

## Acceptance Criteria

- [ ] `tradier_sniffer demo edge_cases --help` shows `--run` option
- [ ] `tradier_sniffer demo edge_cases` (no args) prints all 4 checklists and exits 0
- [ ] All 5 shell scripts exist and are executable
- [ ] `uv run ruff check apps/tradier_sniffer` — zero lint errors
- [ ] `uv run pytest apps/tradier_sniffer` — full suite still green
